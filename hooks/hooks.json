{
  "description": "Trigpoint keeps a ledger honest: it states the plan at session start, and at the end of a working turn it re-runs the commands the ledger already records, unticking anything that stopped being true. Silent in any repository that has not been initialised with .trigpoint/.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session_start.py",
            "shell": "bash",
            "timeout": 20
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" stop.py",
            "shell": "bash",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
